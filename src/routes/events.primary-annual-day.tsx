import { createFileRoute } from "@tanstack/react-router";
import { EventPage } from "@/components/EventPage";
export const Route = createFileRoute("/events/primary-annual-day")({ head: () => ({ meta: [{ title: "Primary Annual Day | PS Matric" }, { name: "description", content: "Primary Annual Day at PS Matriculation Higher Secondary School." }] }), component: () => <EventPage title="Primary Annual Day" /> });